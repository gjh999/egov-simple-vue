# 개발 서버(3001)의 Vite 프록시를 통해 백엔드로 전달된다.
# 프록시를 쓰지 않고 백엔드를 직접 호출하려면 http://localhost:8090/api 로 바꾼다.
VITE_API_BASE=/api

# SNS 간편 로그인 — 공급자 키는 백엔드(application.properties 의 Sns.*)에 넣는다.
# 키를 넣은 뒤 아래를 true 로 바꾸면 로그인 화면에 버튼이 나타난다.
VITE_SNS_ENABLED=false
